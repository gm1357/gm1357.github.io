const projects = [
  {
    id: 1,
    title: "React Chess",
    shortDescription: "A chess game made with React.",
    longDescription: `
A fully interactive chess game built from scratch with React and TypeScript, created using Create React App. The application renders a responsive 8×8 board where two players can take turns moving pieces via a click-to-select, click-to-place interface. It implements the standard movement rules for all six piece types, including pawns' two-square opening move, knights jumping over pieces, and the queen reusing rook and bishop logic via composition. The game enforces turn alternation, validates every move against the rules, detects check and checkmate conditions, and prevents illegal moves that would leave your own king exposed. 

The architecture follows a clean unidirectional data flow (App → Board → Tile → Piece) with a clear separation between rendering and game logic. Each piece type has a dedicated PieceController that implements a shared interface, encapsulating its movement rules alongside its component — a pattern that keeps the codebase modular and easy to extend. The Queen controller demonstrates the composite pattern by combining the Rook and Bishop controllers rather than duplicating logic. Board positions use standard algebraic notation (e.g. "e4"), with a utility layer that converts between string notation and numeric coordinates for move calculations.

On the UI side, the project uses react-transition-group for smooth turn and check/checkmate notifications, and a custom useWindowSize hook to make the board responsive across screen sizes. Move validation runs through a pipeline: candidate moves are generated by the piece controller, then filtered through a legality check that  simulates each move on a copy of the board state to ensure it doesn't leave the player's own king in check. Checkmate detection exhaustively tests whether any piece on the checked side has a legal escape move. The tech stack includes TypeScript for type safety throughout, and React Testing Library with Jest for testing.
`,
    coverImage: "https://github.com/gm1357/react-chess/raw/master/demo.png",
    link: "https://react-chess.vercel.app/",
    github: "https://github.com/gm1357/react-chess",
  },
  {
    id: 2,
    title: "Payment Splitter API",
    shortDescription:
      "A NestJS-based REST API for splitting expenses among group members.",
    longDescription: `
Payment Splitter API is a backend service for splitting expenses between group members, similar to apps like Splitwise. It allows users to create groups, add members, log shared expenses, track individual balances, and settle debts. The API automatically calculates each member's fair share using equal splitting (with cent-level precision to handle remainders), and provides a smart settlement suggestion algorithm that minimizes the number of transactions needed to settle all debts within a group.                                                                                               

The API is built with NestJS (Node.js/TypeScript) following a modular architecture, where each domain feature (users, groups, expenses, balances, settlements) is encapsulated in its own module with dedicated controllers, services, and DTOs. Data persistence is handled by Prisma ORM with PostgreSQL, using a well-normalized relational schema with models for Users, Groups, GroupMembers, Expenses, ExpenseSplits, and Settlements. Authentication is implemented with Passport.js using a local strategy for login and JWT tokens for protecting routes.

The API supports both individual and batch expense creation via CSV upload, with full validation and transactional processing. Email notifications are sent to affected group members when expenses are added, powered by Nodemailer. File storage is integrated through AWS S3 and asynchronous processing through AWS SQS, with LocalStack used as a local mock for development. The codebase enforces soft deletes across all entities, request validation through class-validator with a global ValidationPipe, and auto-generated Swagger/OpenAPI documentation. The project includes both unit tests (with Jest mocks) and integration tests (using Pactum for HTTP assertions against a real database), with Docker Compose orchestrating PostgreSQL, a mail catcher, and LocalStack for a fully self-contained development environment.
`,
    coverImage: "/images/payment-splitter.png",
    link: "https://payment-splitter-api.vercel.app/api-json",
    github: "https://github.com/gm1357/payment-splitter-api",
  },
  {
    id: 3,
    title: "Angular Kanban",
    shortDescription:
      "Angular application for projects management with boards and tasks, like Trello.",
    longDescription: `
Angular Kanban is a web-based project management application inspired by tools like Trello. It provides an interactive Kanban board interface where users can organize work across multiple boards, each containing customizable columns (task lists) and individual task cards. Tasks support Markdown-formatted descriptions with syntax highlighting and emoji support, and can be color-coded for visual categorization. The entire interface supports drag-and-drop interactions — users can reorder columns, move tasks within a list, or transfer them between lists seamlessly.

The application is built with Angular 10 and TypeScript, following a modular, feature-based architecture with clear separation of concerns. The UI layer uses Bootstrap 4 for responsive layout and Font Awesome for iconography, while Angular CDK's drag-drop module powers the drag-and-drop functionality. Task descriptions are rendered through ngx-markdown with Prism.js for code highlighting, and SweetAlert2 provides polished confirmation dialogs for destructive actions. Data is persisted entirely in the browser's localStorage, making it a fully client-side application with no backend dependency.

On the architectural side, the project demonstrates several Angular best practices: a well-organized module hierarchy (Core, Shared, and feature modules), the smart/presentational component pattern for clean data flow, reactive forms via FormBuilder and FormGroup for all user inputs, and singleton services with providedIn: 'root' for data management and persistence. Routing is handled with parameterized routes and dynamic page titles, and the build pipeline leverages Angular CLI with AOT compilation, tree-shaking, and production optimizations.
`,
    coverImage: "https://github.com/gm1357/Angular-Kanban/raw/master/demo.png",
    link: "https://angular-kanban.gmachado1.vercel.app/",
    github: "https://github.com/gm1357/Angular-Kanban",
  },
  {
    id: 4,
    title: "Bidding System",
    shortDescription:
      "A full-stack bidding platform where users can browse item collections, place bids, and have bids accepted or rejected.",
    longDescription: `
A full-stack web application that simulates a real-time bidding platform where users can create collections, place competitive bids, and manage the bid lifecycle through acceptance and rejection workflows. Built as a modern single-page application, it features infinite-scroll pagination, modal-based interactions, and a multi-user experience powered by a user selector.            

The project is built on Next.js 16 with the App Router, React 19, and TypeScript in strict mode. The backend uses Next.js API routes following RESTful conventions, backed by PostgreSQL through Prisma ORM with the PrismaPg driver adapter. The frontend is styled with Tailwind CSS v4 and uses the Geist font family. Local development is containerized with Docker Compose for the database layer.

The application follows a clean separation between API routes and UI components. All prices are stored as integers (cents) to avoid floating-point precision issues. The data model supports soft deletes across all entities via a deletedAt field, enabling auditability and data recovery. A key architectural highlight is the transactional bid acceptance flow: when a bid is accepted, a database transaction atomically updates the winning bid and auto-rejects all competing pending bids, preventing race conditions in concurrent scenarios. The Prisma client uses a singleton pattern cached on globalThis to avoid connection pool exhaustion during development hot reloads.

The project enforces strict code quality through an automated pipeline. Husky pre-commit hooks run ESLint, Prettier, and Prisma schema validation before every commit. Commits follow the Conventional Commits specification, enforced by CommitLint with an interactive Commitizen prompt. GitHub Actions runs three CI workflows on every pull request: linting (Prettier + ESLint + CommitLint), Prisma schema validation, and a full integration test suite. The testing strategy favors end-to-end integration tests that run against a live Next.js server and a real PostgreSQL database — no mocking — ensuring the entire stack works together as expected.
`,
    coverImage: "/images/bidding-system.png",
    link: "https://bidding-system-tau.vercel.app/",
    github: "https://github.com/gm1357/bidding-system",
  },
  {
    id: 5,
    title: "Dynamic Impact Map",
    shortDescription:
      "The Dynamic Impact Map is an interactive visualization tool that showcases a Pastor's cumulative engagement across the United States.",
    longDescription: `
Dynamic Impact Map is a full-stack web application that visualizes a pastor's cumulative engagement across the United States on an interactive map. Each engagement event is represented as an animated flow line between the state where the interaction occurred and the pastor's home state, creating a real-time, data-driven visualization of geographic reach. States are shaded with a blue heatmap gradient based on engagement intensity, and hovering over any state reveals detailed engagement counts.

The frontend is built with Next.js 14, React 18, and TypeScript, using react-simple-maps for SVG-based geographic rendering and Tailwind CSS for styling. Animations are driven by requestAnimationFrame for smooth linear interpolation of coordinates along engagement paths. The UI polls the backend every 60 seconds to pick up new data displaying a countdown timer and automatically animating any newly arrived engagements. State management is kept lightweight with React hooks — no external state library is needed.

The backend is an Express.js API server in TypeScript, backed by a MySQL 8.0 database managed through Prisma ORM. It follows a layered architecture with dedicated controllers, services, and route modules, exposing RESTful endpoints for pastor information, engagement statistics (total and per-state), time-range-filtered engagement data, and GeoJSON map geometry. The entire stack is containerized with Docker Compose for local development, and the project includes seed scripts and random-data generators for realistic demo data.

Key technical highlights include proper interval and animation cleanup to prevent memory leaks, efficient Prisma queries with selective field projection and optional date-range filtering, and a clear separation of concerns across both codebases that keeps each layer independently testable and maintainable.
`,
    coverImage:
      "https://github.com/gm1357/dynamic-impact-map-frontend/raw/main/screen-example.png",
    github: "https://github.com/gm1357/dynamic-impact-map-backend",
  },
];

export default projects;
